# GitHub Upload Checklist

Upload the contents of the final **DaybreakProtocol-GitHub/DaybreakProtocol** folder.

Before submitting the jam entry:

1. Confirm the README animated demo plays on GitHub.
2. Download the repository or release ZIP on another Windows computer.
3. Keep `DaybreakProtocol.exe`, `app`, and `runtime` together and launch the EXE.
4. Add the public repository URL and public video URL to `docs/DEV-SUBMISSION-DRAFT.md`.
5. Publish the DEV post before the challenge deadline.

The included MP4 is a true gameplay video generated from the running JavaFX scene. The GIF is a compact GitHub README preview.
